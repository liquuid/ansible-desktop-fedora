---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: pre-run | update package cache
      tags: always
      dnf: update_cache=yes

- hosts: localhost
  become: true
  roles:
    - zfs
